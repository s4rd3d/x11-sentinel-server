# Ignore self -- we don't want to rebuild images just because a single, but not
# necessarily related file changed
/Dockerfile
/.dockerignore

# Erlang
/_build
/doc*
!/docker
/log*
**/erl_crash.dump

# Git
/.git

# Editor helper files
**/.*.swo
**/.*.swp
/tags.*
